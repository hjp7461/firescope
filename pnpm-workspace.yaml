allowBuilds:
  esbuild: true
# pnpm 10+ requires explicit approval to run package install scripts.
# esbuild ships a native binary built via postinstall; `tauri build` invokes
# `pnpm install` non-interactively so we approve it here.
onlyBuiltDependencies:
  - esbuild
