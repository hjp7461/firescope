rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 개발 에뮬레이터: 읽기만 허용
      allow read: if true;
      allow write: if false;
    }
  }
}
